dimensions: 3
# Linear-Sparse-Grid Configuration
n_grids: 120            # Number of offset grids (reduced for 3D memory) should be a multiple of 24 because crystals or something
grid_size: 4         # Base resolution per grid (32x32x32)
world_size: 100.0
num_particles: 1e6
G: 10.0
dt: 0.0001
particle_mass: 1.0
