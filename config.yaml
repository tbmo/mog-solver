dimensions: 3
# Linear-Sparse-Grid Configuration
n_grids: 60            # Number of offset grids (reduced for 3D memory) should be a multiple of 24 because crystals or something
grid_size: 32         # Base resolution per grid (32x32x32)
world_size: 100.0
# num_particles: 5e5
num_particles: 5e6
G: 1.0
dt: 0.00001
particle_mass: 1.0
